#include "assemblers/poisson_assembler.h"
namespace fem{
  void PoissonAssembler::assemble() {
    // Poisson equation specific assembly logic...
    // You will access the mesh and solver with the protected members mesh_ and solver_
  }
  void PoissonAssembler::setSize(){
  }
}
